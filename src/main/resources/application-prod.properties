# Production Profile Configuration
# H2 Console is disabled for security reasons

spring.datasource.url=jdbc:h2:mem:bolgebaderne;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=bolgebaderne_admin
spring.datasource.password=${DB_PASSWORD:ChangeMeInProduction!2024}

# H2 Console disabled in production for security
spring.h2.console.enabled=false

# Disable SQL logging in production
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
